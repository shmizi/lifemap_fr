// The router (src/app/router) now owns all rendering through RouterProvider in
// main.tsx. This file is intentionally left as a no-op placeholder so the
// scaffold's default export does not linger and mislead. It is not imported.

export default function App() {
  return null
}
